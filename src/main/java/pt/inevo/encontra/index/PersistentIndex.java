package pt.inevo.encontra.index;

/**
 * A class that represents a Index that can stored (saved)
 * @author ricardo
 */
public abstract class PersistentIndex<O extends AbstractObject> implements Index<O>, Storable{
 
}
